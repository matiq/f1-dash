services:
  api:
    build:
      context: .
      dockerfile: dockerfile
      target: api
    environment:
      - RUST_LOG=api=debug
      - ADDRESS=0.0.0.0:80
      - ORIGIN=${FRONTEND_URL}

  realtime:
    build:
      context: .
      dockerfile: dockerfile
      target: realtime
    environment:
      - RUST_LOG=realtime=debug
      - ADDRESS=0.0.0.0:80
      - ORIGIN=${FRONTEND_URL}

  web:
    build:
      context: .
      dockerfile: dockerfile
      target: dashboard
    depends_on:
      - realtime
      - api
    environment:
      - NEXT_PUBLIC_LIVE_URL=${REALTIME_URL}
      - API_URL=${API_URL}
